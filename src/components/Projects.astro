---
// Importar iconos de tecnologías disponibles
import ReactIcon from "../assets/icons/react.astro";
import NodeIcon from "../assets/icons/node.astro";
import DockerIcon from "../assets/icons/docker.astro";
import ExpressIcon from "../assets/icons/express.astro";
import CssIcon from "../assets/icons/css.astro";
import AwsIcon from "../assets/icons/aws.astro";
import TypeScriptIcon from "../assets/icons/typescript.astro";
import NextIcon from "../assets/icons/next-icon.astro";
import GoogleCloudIcon from "../assets/icons/google-cloud.astro";
import FlyIcon from "../assets/icons/fly.astro";
import FirebaseIcon from "../assets/icons/firebase.astro";
import TailwindIcon from "../assets/icons/tailwind.astro";
import TRPCIcon from "../assets/icons/trpc.astro";
import HonoIcon from "../assets/icons/hono.astro";
import BunIcon from "../assets/icons/bun.astro";
import BetterAuthIcon from "../assets/icons/better-auth.astro";
import VercelIcon from "../assets/icons/vercel.astro";
import AstroIcon from "../assets/icons/astro-icon.astro";
import BranchLeftIcon from "../assets/svg/resource13.svg?url";
import BranchRightIcon from "../assets/svg/resource12.svg?url";
// Datos de los proyectos
const projects = [
  {
    title: "IOBOT",
    description:
      "Sistema omnicanal que centraliza la gestión de canales como WhatsApp, Instagram y Messenger, ofreciendo funcionalidades avanzadas para CRM, automatización de campañas, atención a clientes por call center y análisis de rendimiento.",
    technologies: [
      "React",
      "Node.js",
      "Docker",
      "AWS",
      "TypeScript",
      "Vercel",
      "Express",
      "Cloud",
      "Tailwind",
    ],
    image: "/mockup_iobot.png",
    link: "https://app.iobot.chat",
    github: "#",
  },
  {
    title: "Embedded WhatsApp-qr API",
    description:
      "Servicio HTTP en Node.js/Express que gestiona sesión de WhatsApp Web, expone endpoints para enviar mensajes y sirve imágenes QR de vinculación. Genera eventos hacia un backend externo mediante solicitudes POST.",
    technologies: ["Fly", "Docker", "Node.js", "Express"],
    image: "/mockup_qr.png",
    link: "https://github.com/DavidMrtz8/whatsApp-qr",
    github: "#",
  },
  {
    title: "Wendy's Ecommerce",
    description:
      "Sistema de catálogos digital que permite a los usuarios explorar el menú, personalizar productos y gestionar sus órdenes de forma rápida y sencilla, optimizando la experiencia de compra desde cualquier dispositivo.",
    technologies: [
      "Next.js",
      "TypeScript",
      "Tailwind",
      "Firebase",
      "Vercel",
      "TRPC",
      "Hono",
      "Bun",
    ],
    image: "/wendys.png",
    link: "https://ordenar.wendys.hn/catalogs/for-you",
    github: "#",
  },

  {
    title: "Firebase Adapter Better Auth",
    description:
      "Demostracion de como usar el adaptador de Firebase para Better Auth, proporcionando autenticación completa con email y contraseña usando Firebase Realtime Database.",
    technologies: [
      "Next.js",
      "TypeScript",
      "Tailwind",
      "Firebase",
      "Vercel",
      "Better Auth",
    ],
    image: "/mockup_firebase_auth.png",
    link: "https://github.com/DavidMrtz8/firebase-adapter-better-auth",
    github: "https://github.com/DavidMrtz8/firebase-adapter-better-auth",
  },
];

// Mapeo de tecnologías a iconos (usando iconos disponibles o emojis como fallback)
const techIcons: Record<string, any> = {
  React: ReactIcon,
  "Node.js": NodeIcon,
  Docker: DockerIcon,
  Express: ExpressIcon,
  PostgreSQL: "🐘",
  AWS: AwsIcon,
  Astro: AstroIcon,
  CSS: CssIcon,
  TypeScript: TypeScriptIcon,
  "Next.js": NextIcon,
  Vercel: VercelIcon,
  Cloud: GoogleCloudIcon,
  Fly: FlyIcon,
  Firebase: FirebaseIcon,
  Tailwind: TailwindIcon,
  TRPC: TRPCIcon,
  Hono: HonoIcon,
  Bun: BunIcon,
  "Better Auth": BetterAuthIcon,
};
---

<section class="w-full" id="projects">
  <!-- Título de la sección -->
  <div class="text-left mb-8 md:mb-16 flex items-center justify-center px-4 container mx-auto pt-8 md:pt-16">
    <img src={BranchLeftIcon} alt="branch-left" class="w-10 h-10" />
    <h2 class="text-2xl md:text-3xl lg:text-4xl font-bold text-gray-800">
      Selected Projects
    </h2>
    <img src={BranchRightIcon} alt="branch-right" class="w-10 h-10" />
  </div>

  <!-- First Project Section (IOBOT) -->
  <div class="relative bg-gray-200 py-16 md:py-24">
    <div class="container mx-auto px-4">
      <div class="flex flex-col lg:flex-row items-center gap-8 lg:gap-16">
        {/* Laptop Mockup */}
        <div class="flex-1 max-w-2xl">
          {projects[0].image && (
            <img
              src={projects[0].image}
              alt={projects[0].title}
              class="w-full h-auto object-contain"
            />
          )}
        </div>
        
        {/* Project Info Overlay */}
        <div class="flex-1 max-w-lg bg-white p-8 rounded-lg shadow-lg relative z-10">
          <h3 class="text-2xl md:text-3xl font-bold text-gray-800 mb-4">
            {projects[0].title}
          </h3>
          <p class="text-sm md:text-base text-gray-600 leading-relaxed mb-6">
            {projects[0].description}
          </p>
          
          {/* Technologies */}
          <div class="flex flex-wrap gap-3">
            {projects[0].technologies.map((tech) => {
              const IconComponent = techIcons[tech];
              return (
                <span class="inline-flex items-center justify-center p-2 rounded-lg bg-gray-50">
                  {typeof IconComponent === "string" ? (
                    <span class="text-gray-700 text-lg">
                      {IconComponent}
                    </span>
                  ) : (
                    <IconComponent class="w-6 h-6 text-gray-700" />
                  )}
                </span>
              );
            })}
          </div>
        </div>
      </div>
    </div>
  </div>

  <!-- Second Project Section (WhatsApp QR API) -->
  <div class="relative bg-amber-50 py-16 md:py-24">
    {/* Ver Proyecto Button */}
    <div class="absolute top-8 left-8 z-20">
      <a
        href={projects[1].link}
        target="_blank"
        class="inline-flex items-center px-6 py-3 bg-blue-900 text-white rounded-lg hover:bg-blue-800 transition-colors font-medium shadow-lg"
      >
        Ver Proyecto
      </a>
    </div>
    
    <div class="container mx-auto px-4">
      <div class="flex flex-col lg:flex-row-reverse items-center gap-8 lg:gap-16">
        {/* Laptop Mockup */}
        <div class="flex-1 max-w-2xl">
          {projects[1].image && (
            <img
              src={projects[1].image}
              alt={projects[1].title}
              class="w-full h-auto object-contain"
            />
          )}
        </div>
        
        {/* Project Info */}
        <div class="flex-1 max-w-lg">
          <h3 class="text-2xl md:text-3xl font-bold text-gray-800 mb-4">
            {projects[1].title}
          </h3>
          <p class="text-sm md:text-base text-gray-600 leading-relaxed mb-6">
            {projects[1].description}
          </p>
          
          {/* Technologies */}
          <div class="flex flex-wrap gap-3">
            {projects[1].technologies.map((tech) => {
              const IconComponent = techIcons[tech];
              return (
                <span class="inline-flex items-center justify-center p-2 rounded-lg bg-white shadow-sm">
                  {typeof IconComponent === "string" ? (
                    <span class="text-gray-700 text-lg">
                      {IconComponent}
                    </span>
                  ) : (
                    <IconComponent class="w-6 h-6 text-gray-700" />
                  )}
                </span>
              );
            })}
          </div>
        </div>
      </div>
    </div>
  </div>
</section>
